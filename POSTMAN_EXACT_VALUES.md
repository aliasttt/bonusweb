# 📮 راهنمای دقیق Postman - مقادیر دقیق

این فایل شامل مقادیر دقیقی است که باید در Postman وارد کنی تا خطای 500 نگیری.

---

## 🔐 POST `/api/accounts/register/` - ثبت نام

### تنظیمات Postman:

#### 1. Method:
```
POST
```

#### 2. URL:
```
http://127.0.0.1:8000/api/accounts/register/
```

#### 3. Headers:
| Key | Value |
|-----|-------|
| `Content-Type` | `application/json` |

**⚠️ مهم**: حتماً این header را اضافه کن، وگرنه خطای 400 می‌گیری!

---

### 4. Body (Raw JSON):

#### ✅ مثال 1: حداقل فیلدها (الزامی)
```json
{
  "username": "testuser123",
  "password": "TestPass123!",
  "password_confirm": "TestPass123!"
}
```

#### ✅ مثال 2: کامل (با تمام فیلدها)
```json
{
  "username": "ali_ahmadi",
  "password": "AliPassword123!",
  "password_confirm": "AliPassword123!",
  "email": "ali@example.com",
  "first_name": "علی",
  "last_name": "احمدی",
  "phone": "09123456789",
  "role": "customer"
}
```

#### ✅ مثال 3: بدون ایمیل و نام (کاربر ساده)
```json
{
  "username": "user123",
  "password": "UserPass123!",
  "password_confirm": "UserPass123!",
  "phone": "09123456789"
}
```

---

## 📋 قوانین دقیق هر فیلد:

### ✅ `username` (الزامی):
- **نوع**: string
- **حداکثر**: 150 کاراکتر
- **ویژگی**: باید unique باشد (اگر قبلاً استفاده شده باشد، خطا می‌دهد)
- **مثال‌های صحیح**:
  - `"testuser123"`
  - `"ali_ahmadi"`
  - `"user2025"`
  - `"myusername"`

### ✅ `password` (الزامی):
- **نوع**: string
- **قوانین Django Password Validators**:
  - ✅ حداقل 8 کاراکتر
  - ✅ حداقل یک عدد (0-9)
  - ✅ حداقل یک حرف بزرگ (A-Z)
  - ✅ حداقل یک حرف کوچک (a-z)
  - ✅ حداقل یک کاراکتر خاص (!@#$%^&*)
- **مثال‌های صحیح**:
  - `"TestPass123!"` ✅
  - `"MyPassword456@"` ✅
  - `"Strong1234$"` ✅
- **مثال‌های اشتباه**:
  - `"12345678"` ❌ (فقط عدد)
  - `"password"` ❌ (فقط حرف کوچک)
  - `"PASSWORD"` ❌ (فقط حرف بزرگ)
  - `"Pass123"` ❌ (کمتر از 8 کاراکتر)

### ✅ `password_confirm` (الزامی):
- **نوع**: string
- **ویژگی**: باید دقیقاً مثل `password` باشد
- **مثال**:
  - اگر `password` = `"TestPass123!"`
  - پس `password_confirm` = `"TestPass123!"` (دقیقاً یکسان)

### ❌ `email` (اختیاری):
- **نوع**: string
- **فرمت**: باید format ایمیل معتبر باشد
- **مثال‌های صحیح**:
  - `"ali@example.com"` ✅
  - `"user@gmail.com"` ✅
  - `""` ✅ (می‌توانی خالی بگذاری)
- **مثال‌های اشتباه**:
  - `"notanemail"` ❌
  - `"user@com"` ❌

### ❌ `first_name` (اختیاری):
- **نوع**: string
- **محدودیت**: می‌توانی خالی بگذاری
- **مثال**:
  - `"علی"`
  - `"Ali"`
  - `""` (خالی)

### ❌ `last_name` (اختیاری):
- **نوع**: string
- **محدودیت**: می‌توانی خالی بگذاری
- **مثال**:
  - `"احمدی"`
  - `"Ahmadi"`
  - `""` (خالی)

### ❌ `phone` (اختیاری):
- **نوع**: string
- **حداکثر**: 32 کاراکتر
- **مثال**:
  - `"09123456789"`
  - `"02112345678"`
  - `""` (خالی)

### ❌ `role` (اختیاری):
- **نوع**: string
- **مقادیر مجاز**:
  - `"customer"` (پیش‌فرض) ✅
  - `"business_owner"` ✅
  - `"admin"` ✅
  - `"superuser"` ❌ (نمی‌توانی در ثبت نام استفاده کنی، حتی اگر بفرستی به "customer" تبدیل می‌شود)
- **پیش‌فرض**: اگر نگذاری، خودکار `"customer"` می‌شود

---

## 🎯 مثال‌های کامل Copy-Paste:

### مثال 1: کاربر ساده (توصیه می‌شود برای تست)
```json
{
  "username": "testuser2025",
  "password": "Test1234!",
  "password_confirm": "Test1234!"
}
```

### مثال 2: کاربر کامل ایرانی
```json
{
  "username": "ali_iranian",
  "password": "AliPass123!",
  "password_confirm": "AliPass123!",
  "email": "ali@example.com",
  "first_name": "علی",
  "last_name": "احمدی",
  "phone": "09123456789"
}
```

### مثال 3: کاربر انگلیسی
```json
{
  "username": "john_doe",
  "password": "JohnDoe123!",
  "password_confirm": "JohnDoe123!",
  "email": "john@example.com",
  "first_name": "John",
  "last_name": "Doe",
  "phone": "09123456789"
}
```

---

## ⚠️ خطاهای رایج و راه‌حل:

### خطای 1: "This field is required"
**مشکل**: فیلدهای الزامی ارسال نشده‌اند
**راه‌حل**: مطمئن شو که `username`، `password` و `password_confirm` را فرستادی

### خطای 2: "Passwords don't match"
**مشکل**: `password` و `password_confirm` یکسان نیستند
**راه‌حل**: مطمئن شو که هر دو یکسان هستند

### خطای 3: "This password is too short. It must contain at least 8 characters."
**مشکل**: password کمتر از 8 کاراکتر است
**راه‌حل**: از password قوی‌تر استفاده کن (مثال: `"Test1234!"`)

### خطای 4: "This password is too common."
**مشکل**: password خیلی ساده است (مثل "password123")
**راه‌حل**: از password پیچیده‌تر استفاده کن

### خطای 5: "A user with that username already exists."
**مشکل**: username قبلاً استفاده شده است
**راه‌حل**: username دیگری انتخاب کن (مثال: `"testuser2025"`)

### خطای 6: 500 Internal Server Error
**مشکل**: معمولاً خطای سرور یا مشکل در دیتابیس
**راه‌حل**: 
- مطمئن شو که server در حال اجرا است
- لاگ‌های سرور را بررسی کن
- مطمئن شو که دیتابیس migrate شده است

---

## 📝 مراحل تست در Postman:

### مرحله 1: تنظیمات اولیه
1. Method را روی **POST** تنظیم کن
2. URL را وارد کن: `http://127.0.0.1:8000/api/accounts/register/`

### مرحله 2: تنظیم Headers
1. تب **Headers** را باز کن
2. Key: `Content-Type`
3. Value: `application/json`
4. روی **Save** کلیک کن

### مرحله 3: تنظیم Body
1. تب **Body** را باز کن
2. گزینه **raw** را انتخاب کن
3. در dropdown سمت راست، **JSON** را انتخاب کن
4. یکی از مثال‌های بالا را کپی و paste کن
5. مطمئن شو که JSON syntax درست است (بدون trailing comma)

### مرحله 4: ارسال درخواست
1. روی **Send** کلیک کن
2. پاسخ را بررسی کن

---

## ✅ پاسخ موفق (201 Created):

```json
{
  "user": {
    "id": 5,
    "username": "testuser2025",
    "first_name": "",
    "last_name": "",
    "email": "",
    "date_joined": "2025-11-02T20:15:30Z",
    "is_active": true
  },
  "profile": {
    "id": 1,
    "role": "customer",
    "phone": "",
    "business_name": "",
    "is_active": true,
    "last_login_ip": null,
    "created_at": "2025-11-02T20:15:30Z",
    "updated_at": "2025-11-02T20:15:30Z",
    "business_type": "",
    "business_address": "",
    "business_phone": "",
    "total_logins": 0,
    "last_activity": null
  }
}
```

---

## 🔥 نکات نهایی:

1. ✅ همیشه `Content-Type: application/json` را در Headers اضافه کن
2. ✅ مطمئن شو که password حداقل 8 کاراکتر و شامل عدد و حرف بزرگ و کوچک است
3. ✅ `password` و `password_confirm` باید دقیقاً یکسان باشند
4. ✅ username باید unique باشد (اگر خطا داد، username دیگری امتحان کن)
5. ✅ JSON syntax را بررسی کن (بدون trailing comma در آخر)

---

## 💡 مثال سریع برای Copy-Paste:

کپی کن و در Postman Body قرار بده:

```json
{
  "username": "testuser2025",
  "password": "Test1234!",
  "password_confirm": "Test1234!"
}
```

یا برای کاربر کامل:

```json
{
  "username": "ali_test",
  "password": "AliTest123!",
  "password_confirm": "AliTest123!",
  "email": "ali@test.com",
  "first_name": "علی",
  "last_name": "احمدی",
  "phone": "09123456789"
}
```

---

**نکته**: اگر هنوز خطا می‌گیری، پاسخ کامل خطا را بفرست تا بررسی کنم!


