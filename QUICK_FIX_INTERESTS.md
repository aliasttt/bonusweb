# راه حل سریع: رفع خطای interests

## ⚡ روش سریع: استفاده از Dashboard Scalingo

### مرحله 1: ورود به Dashboard
1. مرورگر را باز کنید
2. به آدرس بروید: **https://dashboard.scalingo.com**
3. لاگین کنید

### مرحله 2: انتخاب اپلیکیشن
1. اپلیکیشن **`mywebsite`** را انتخاب کنید

### مرحله 3: اجرای Migration
1. در منوی سمت چپ، به بخش **"One-off containers"** یا **"Run command"** بروید
2. در کادر دستور، این را وارد کنید:
   ```
   python manage.py migrate accounts
   ```
3. روی دکمه **"Run"** کلیک کنید

### مرحله 4: بررسی نتیجه
- باید پیام موفقیت را ببینید
- سپس صفحه `/partners/dashboard/` را دوباره تست کنید
- خطا باید برطرف شده باشد

---

## روش 2: نصب دستی Scalingo CLI

اگر می‌خواهید از CLI استفاده کنید:

### مرحله 1: دانلود Scalingo CLI
1. به آدرس بروید: **https://cli.scalingo.com/install**
2. یا مستقیماً از این لینک دانلود کنید:
   - Windows 64-bit: https://github.com/Scalingo/cli/releases/latest/download/scalingo_windows_amd64.exe
3. فایل را دانلود کنید

### مرحله 2: نصب
1. فایل دانلود شده را اجرا کنید
2. یا فایل را در یک پوشه (مثلاً `C:\tools\scalingo\`) قرار دهید
3. مسیر را به PATH اضافه کنید

### مرحله 3: استفاده
```powershell
# لاگین
scalingo login

# اجرای migration
scalingo --app mywebsite run python manage.py migrate accounts
```

---

## ✅ توصیه

**از Dashboard استفاده کنید** چون:
- ✅ نیاز به نصب CLI ندارد
- ✅ سریع‌تر است
- ✅ رابط کاربری ساده‌ای دارد
- ✅ فقط چند کلیک است

---

## 📝 توضیحات

- Migration فایل `accounts/migrations/0004_profile_interests.py` وجود دارد
- این migration ستون `interests` را به جدول `accounts_profile` اضافه می‌کند
- بعد از اجرای migration، خطا برطرف می‌شود

